# 云端页「手动粘贴 PAT」入口 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为云端页增加「粘贴 Token（PAT）」入口，保存前校验有效性 + 写权限，PAT 复用现有 GitHubToken 存储，根治 device-flow token 反复失效问题。

**Architecture:** 三层小改动：(1) `GitHubApiClient` 新增纯逻辑静态校验方法（可单测）+ `ValidatePatAsync` 异步方法；(2) 新增轻量输入弹窗 `TextInputDialog`；(3) `MainForm.Cloud.cs` 底部按钮区改为 1×2 表格，未授权态并列「授权 GitHub / 粘贴 Token（PAT）」。

**Tech Stack:** C# / .NET 9 / WinForms / xUnit（测试在独立目录 `TraeCheckin.Tests`，不在仓库内，用 `-p:OutputPath` 覆盖运行避免锁定正在运行的 exe）。

**测试运行命令（贯穿全计划）：**
```
& "C:\Program Files\dotnet\dotnet.exe" test --nologo -p:OutputPath="$env:TEMP\traebuild_pat" -v q
```
**编译命令：** `& "C:\Program Files\dotnet\dotnet.exe" build TraeCheckin.csproj -c Release --nologo`

**关键既有代码定位：**
- `Api/GitHubApiClient.cs` — 常量 `SourceOwner="star620"`、`SourceRepo="TRAE-Automatic-sign-in"`（L61-63）；`GetLoginAsync`（L134-145）；`SendApiAsync`（L385-391）。
- `Forms/MainForm.Cloud.cs` — `RefreshCloudState`（L87-104）；`BuildCloudAction`（L57-84）；`SetCloudLog`（L388-394）。
- 主题色为 `MainForm` 私有静态字段（Accent/CardBg/TextMuted/ContentBg），partial 文件可直接访问；独立控件（`Controls/HistoryChart.cs` 先例）自带同名色值。

---

## Task 1: `GitHubApiClient` 静态校验方法（TDD）

**Files:**
- Modify: `Api/GitHubApiClient.cs`（`public class GitHubApiClient` 内，`BuildWorkflowError` 之后）
- Test: `TraeCheckin.Tests/PatValidationTests.cs`（新建）

- [ ] **Step 1: 写失败测试**

创建 `c:\Users\星梦\Desktop\插件开发\TraeCheckin.Tests\PatValidationTests.cs`：

```csharp
using TraeCheckin;

namespace TraeCheckin.Tests;

/// <summary>
/// PAT 校验的纯逻辑测试：Token 预检、permissions.push JSON 解析、权限不足提示文案。
/// </summary>
public class PatValidationTests
{
    // ---- IsPlausibleToken ----
    [Theory]
    [InlineData(null)]
    [InlineData("")]
    [InlineData("   ")]
    [InlineData("short-token")]
    public void Token为空或过短_应判定不可用(string? token)
    {
        Assert.False(GitHubApiClient.IsPlausibleToken(token));
    }

    [Fact]
    public void FineGrainedPat格式_应判定可用()
    {
        Assert.True(GitHubApiClient.IsPlausibleToken("github_pat_" + new string('x', 80)));
    }

    [Fact]
    public void 旧OAuthToken格式_应判定可用()
    {
        Assert.True(GitHubApiClient.IsPlausibleToken("gho_" + new string('x', 40)));
    }

    // ---- ParsePermissionsPush ----
    [Fact]
    public void permissionsPush为true_返回true()
    {
        var json = """{"permissions":{"admin":true,"push":true,"pull":true}}""";
        Assert.True(GitHubApiClient.ParsePermissionsPush(json));
    }

    [Fact]
    public void permissionsPush为false_返回false()
    {
        var json = """{"permissions":{"admin":false,"push":false}}""";
        Assert.False(GitHubApiClient.ParsePermissionsPush(json));
    }

    [Fact]
    public void 无permissions字段_返回false()
    {
        var json = """{"full_name":"star620/TRAE-Automatic-sign-in"}""";
        Assert.False(GitHubApiClient.ParsePermissionsPush(json));
    }

    [Fact]
    public void permissions缺少push_返回false()
    {
        var json = """{"permissions":{"admin":true,"pull":true}}""";
        Assert.False(GitHubApiClient.ParsePermissionsPush(json));
    }

    [Fact]
    public void 非法JSON_返回false而不抛异常()
    {
        Assert.False(GitHubApiClient.ParsePermissionsPush("not-json{"));
    }

    // ---- BuildPatScopeHint ----
    [Fact]
    public void 权限提示文案_应包含关键勾选项()
    {
        var hint = GitHubApiClient.BuildPatScopeHint();
        Assert.Contains("Contents", hint);
        Assert.Contains("Workflows", hint);
        Assert.Contains("Read and write", hint);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `dotnet test`（测试命令见计划头，cwd=`TraeCheckin.Tests`）
Expected: 编译失败，`GitHubApiClient` 不含 `IsPlausibleToken` / `ParsePermissionsPush` / `BuildPatScopeHint`。

- [ ] **Step 3: 最小实现**

在 `Api/GitHubApiClient.cs` 中 `BuildWorkflowError` 方法（L194-197）之后插入：

```csharp
    /// <summary>PAT 校验结果。</summary>
    public class PatValidation
    {
        public bool IsValid { get; set; }
        public string? Login { get; set; }
        public bool CanWrite { get; set; }
        public string? Error { get; set; }
    }
```

> 注意：`PatValidation` 必须放在 `GitHubApiClient` 类**外部**（与 `DeploymentStatus` 同级，见 L26-38），否则无法在测试中构造。

在 `BuildWorkflowError` 方法体后、类内插入：

```csharp
    /// <summary>预检：是否像是一段可用的 GitHub token（PAT 或 OAuth token 均放行）。</summary>
    public static bool IsPlausibleToken(string? token)
        => !string.IsNullOrWhiteSpace(token) && token.Trim().Length >= 20;

    /// <summary>解析 GET /repos/{owner}/{repo} 响应中的 permissions.push（无写权限返回 false）。</summary>
    public static bool ParsePermissionsPush(string json)
    {
        try
        {
            using var doc = System.Text.Json.JsonDocument.Parse(json);
            return ParsePermissionsPush(doc.RootElement);
        }
        catch (System.Text.Json.JsonException) { return false; }
    }

    /// <summary>解析 permissions.push 的 JsonElement 版本。</summary>
    public static bool ParsePermissionsPush(System.Text.Json.JsonElement root)
    {
        if (!root.TryGetProperty("permissions", out var perms) || perms.ValueKind != System.Text.Json.JsonValueKind.Object)
            return false;
        return perms.TryGetProperty("push", out var push) && push.ValueKind == System.Text.Json.JsonValueKind.True;
    }

    /// <summary>PAT 对目标仓库写权限不足时的指引文案。</summary>
    public static string BuildPatScopeHint()
        => "该 Token 对目标仓库没有写权限。请确认：1) Repository access 已包含 TRAE-Automatic-sign-in"
           + "（若已 fork 还需包含你自己的 fork 仓库）；"
           + "2) Permissions 中 Actions / Contents / Pull requests / Secrets / Workflows 均为 Read and write，"
           + "Metadata 为只读（自动带出）。修改后需重新生成 Token。";
```

- [ ] **Step 4: 运行测试确认通过**

Run: `dotnet test`
Expected: 11/11 通过（`PatValidationTests`），全量约 36。

- [ ] **Step 5: 提交**

```bash
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" add Api/GitHubApiClient.cs
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" commit -m "feat: GitHubApiClient 新增 PAT 校验静态方法（预检/写权限解析/提示文案）"
```
> 测试文件在仓库外，无需 add。

---

## Task 2: `ValidatePatAsync` 异步校验方法

**Files:**
- Modify: `Api/GitHubApiClient.cs`（`GetLatestRunAsync` 之后、`// ---------- 部署状态检测 ----------` 之前）

- [ ] **Step 1: 实现方法**

在 `GetLatestRunAsync` 方法结束（L317）后插入：

```csharp
    /// <summary>
    /// 校验手动粘贴的 Token：GET /user 确认有效并取 login，再按 login 分派探测目标仓库写权限。
    /// owner 用源仓库校验；非 owner 若尚未 fork（404）放行（fork 后对自有仓库天然可写）。
    /// </summary>
    public async Task<PatValidation> ValidatePatAsync(string pat)
    {
        string token = (pat ?? "").Trim();
        if (!IsPlausibleToken(token))
            return new PatValidation { IsValid = false, Error = "请输入有效的 GitHub Token（github_pat_ 或 gho_ 开头，长度不少于 20）" };

        try
        {
            string login = await GetLoginAsync(token) ?? "";
            if (string.IsNullOrEmpty(login))
            {
                LastError = "Token 无效或已被撤销";
                return new PatValidation { IsValid = false, Error = LastError };
            }

            bool isOwner = string.Equals(login, SourceOwner, StringComparison.OrdinalIgnoreCase);
            string repoPath = isOwner
                ? $"/repos/{SourceOwner}/{SourceRepo}"
                : $"/repos/{login}/{SourceRepo}";

            using var resp = await SendApiAsync(HttpMethod.Get, repoPath, token);
            if (resp.StatusCode == System.Net.HttpStatusCode.NotFound && !isOwner)
            {
                // 尚未 fork：放行，fork 后对自有仓库天然可写
                return new PatValidation { IsValid = true, Login = login, CanWrite = false };
            }
            if (!resp.IsSuccessStatusCode)
            {
                LastError = $"HTTP {(int)resp.StatusCode}";
                return new PatValidation { IsValid = false, Login = login, Error = LastError };
            }

            var json = await resp.Content.ReadAsStringAsync();
            if (!ParsePermissionsPush(json))
                return new PatValidation { IsValid = false, Login = login, Error = BuildPatScopeHint() };

            return new PatValidation { IsValid = true, Login = login, CanWrite = true };
        }
        catch (Exception ex)
        {
            LastError = "网络错误：" + ex.Message;
            return new PatValidation { IsValid = false, Error = LastError };
        }
    }
```

- [ ] **Step 2: 编译确认 0 错误**

Run: `dotnet build TraeCheckin.csproj -c Release`
Expected: `0 个错误`。

- [ ] **Step 3: 提交**

```bash
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" add Api/GitHubApiClient.cs
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" commit -m "feat: GitHubApiClient 新增 ValidatePatAsync（有效性+写权限校验，未 fork 放行）"
```

---

## Task 3: `TextInputDialog` 输入弹窗控件

**Files:**
- Create: `Controls/TextInputDialog.cs`

- [ ] **Step 1: 创建控件**

```csharp
using System.Drawing;
using System.Windows.Forms;

namespace TraeCheckin;

/// <summary>
/// 轻量文本输入弹窗：说明 + 掩码输入框 + 显示/隐藏 + 粘贴按钮 + 确定/取消。
/// 用于云端页粘贴 GitHub Token（PAT）。
/// </summary>
public class TextInputDialog : Form
{
    private static readonly Color Accent = Color.FromArgb(59, 130, 246);
    private static readonly Color TextMain = Color.FromArgb(15, 23, 42);
    private static readonly Color TextMuted = Color.FromArgb(100, 116, 139);

    private readonly TextBox _txt = new() { UseSystemPasswordChar = true };
    private readonly CheckBox _chkShow = new() { Text = "显示", AutoSize = true };

    /// <summary>确定后返回用户输入（含密码掩码解除后的值）。</summary>
    public string? Value { get; private set; }

    public TextInputDialog(string title, string hint)
    {
        Text = title;
        Font = new Font("Segoe UI", 9);
        FormBorderStyle = FormBorderStyle.FixedDialog;
        MaximizeBox = false;
        MinimizeBox = false;
        ShowInTaskbar = false;
        StartPosition = FormStartPosition.CenterParent;
        ClientSize = new Size(540, 280);
        BackColor = Color.White;
        ForeColor = TextMain;

        var root = new TableLayoutPanel { Dock = DockStyle.Fill, Padding = new Padding(20), ColumnCount = 1, RowCount = 4, BackColor = Color.White };
        root.RowStyles.Add(new RowStyle(SizeType.Absolute, 96));   // 说明
        root.RowStyles.Add(new RowStyle(SizeType.Absolute, 34));   // 输入行
        root.RowStyles.Add(new RowStyle(SizeType.Absolute, 30));   // 显示/粘贴 工具行
        root.RowStyles.Add(new RowStyle(SizeType.Percent, 100));   // 按钮行

        var lblHint = new Label
        {
            Dock = DockStyle.Fill,
            Text = hint,
            ForeColor = TextMuted,
            AutoSize = false,
            WordWrap = true,
            TextAlign = ContentAlignment.TopLeft
        };

        _txt.Dock = DockStyle.Fill;
        _txt.BorderStyle = BorderStyle.FixedSingle;
        _txt.Font = new Font("Consolas", 10);

        var btnPaste = new Button { Text = "从剪贴板粘贴", AutoSize = false, Height = 26, FlatStyle = FlatStyle.Flat };
        btnPaste.Click += (_, _) =>
        {
            try { if (Clipboard.ContainsText()) _txt.Text = Clipboard.GetText().Trim(); }
            catch { /* 剪贴板不可用时忽略 */ }
        };
        _chkShow.CheckedChanged += (_, _) => _txt.UseSystemPasswordChar = !_chkShow.Checked;

        var toolRow = new FlowLayoutPanel { Dock = DockStyle.Fill, FlowDirection = FlowDirection.LeftToRight, WrapContents = false };
        toolRow.Controls.Add(_chkShow);
        toolRow.Controls.Add(btnPaste);

        var ok = new Button { Text = "确定", DialogResult = DialogResult.OK, Width = 96, Height = 34, BackColor = Accent, ForeColor = Color.White, FlatStyle = FlatStyle.Flat };
        var cancel = new Button { Text = "取消", DialogResult = DialogResult.Cancel, Width = 96, Height = 34, FlatStyle = FlatStyle.Flat };

        var btnRow = new FlowLayoutPanel { Dock = DockStyle.Bottom, FlowDirection = FlowDirection.RightToLeft, WrapContents = false };
        btnRow.Controls.Add(cancel);
        btnRow.Controls.Add(ok);

        root.Controls.Add(lblHint, 0, 0);
        root.Controls.Add(_txt, 0, 1);
        root.Controls.Add(toolRow, 0, 2);
        root.Controls.Add(btnRow, 0, 3);

        Controls.Add(root);
        AcceptButton = ok;
        CancelButton = cancel;

        ok.Click += (_, _) =>
        {
            if (!string.IsNullOrWhiteSpace(_txt.Text)) Value = _txt.Text.Trim();
        };
    }
}
```

- [ ] **Step 2: 编译确认 0 错误**

Run: `dotnet build TraeCheckin.csproj -c Release`
Expected: `0 个错误`（WinForms 程序默认隐式 using 含 WinForms 需确认 csproj 已 enable；主项目为 net9.0-windows + UseWindowsForms，若缺 using 按报错补充）。

- [ ] **Step 3: 提交**

```bash
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" add Controls/TextInputDialog.cs
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" commit -m "feat: 新增 TextInputDialog 文本输入弹窗（掩码+粘贴）"
```

---

## Task 4: 云端页双按钮集成

**Files:**
- Modify: `Forms/MainForm.Cloud.cs`

- [ ] **Step 1: 新增字段（L19 `_btnCloudAction` 定义旁）**

在文件顶部字段区（`_btnCloudAction` 行后）加入：

```csharp
    private readonly Button _btnUsePat = new();
    private readonly TableLayoutPanel _cloudActionRow = new();
```

- [ ] **Step 2: 改造 `BuildCloudAction()`（替换 L57-84 整方法）**

原方法内容整体替换为：

```csharp
    private Control BuildCloudAction()
    {
        var panel = new Panel { Dock = DockStyle.Fill, BackColor = CardBg };

        _lblCloudState.Dock = DockStyle.Top;
        _lblCloudState.Height = 40;
        _lblCloudState.Font = new Font("Segoe UI", 10, FontStyle.Bold);
        _lblCloudState.ForeColor = TextMain;
        _lblCloudState.TextAlign = ContentAlignment.MiddleLeft;

        _lblCloudCode.Dock = DockStyle.Top;
        _lblCloudCode.Height = 30;
        _lblCloudCode.Font = new Font("Consolas", 12, FontStyle.Bold);
        _lblCloudCode.ForeColor = Accent;
        _lblCloudCode.TextAlign = ContentAlignment.MiddleLeft;
        _lblCloudCode.Visible = false;

        _btnCloudAction.Dock = DockStyle.Fill;
        _btnCloudAction.FlatStyle = FlatStyle.Flat;
        _btnCloudAction.FlatAppearance.BorderSize = 0;
        _btnCloudAction.BackColor = Accent;
        _btnCloudAction.ForeColor = Color.White;
        _btnCloudAction.Font = new Font("Segoe UI", 11, FontStyle.Bold);
        _btnCloudAction.Cursor = Cursors.Hand;
        _btnCloudAction.Click += async (_, _) => await OnCloudActionAsync();

        _btnUsePat.Dock = DockStyle.Fill;
        _btnUsePat.Text = "粘贴 Token（PAT）";
        _btnUsePat.FlatStyle = FlatStyle.Flat;
        _btnUsePat.FlatAppearance.BorderColor = Accent;
        _btnUsePat.FlatAppearance.BorderSize = 1;
        _btnUsePat.BackColor = CardBg;
        _btnUsePat.ForeColor = Accent;
        _btnUsePat.Font = new Font("Segoe UI", 10, FontStyle.Bold);
        _btnUsePat.Cursor = Cursors.Hand;
        _btnUsePat.Click += async (_, _) => await UsePatAsync();

        _cloudActionRow.Dock = DockStyle.Bottom;
        _cloudActionRow.Height = 40;
        _cloudActionRow.ColumnCount = 2;
        _cloudActionRow.RowCount = 1;
        _cloudActionRow.ColumnStyles.Add(new ColumnStyle(SizeType.Percent, 50));
        _cloudActionRow.ColumnStyles.Add(new ColumnStyle(SizeType.Percent, 50));
        _cloudActionRow.Controls.Add(_btnCloudAction, 0, 0);
        _cloudActionRow.Controls.Add(_btnUsePat, 1, 0);

        panel.Controls.Add(_cloudActionRow);
        panel.Controls.Add(_lblCloudCode);
        panel.Controls.Add(_lblCloudState);
        return panel;
    }

    /// <summary>控制底部按钮区形态：patOption=true 时双按钮并列，false 时主按钮占满两列。</summary>
    private void UpdateCloudActionUi(bool patOption)
    {
        if (patOption)
        {
            _btnUsePat.Visible = true;
            _cloudActionRow.SetColumnSpan(_btnCloudAction, 1);
        }
        else
        {
            _btnUsePat.Visible = false;
            _cloudActionRow.SetColumnSpan(_btnCloudAction, 2);
        }
    }

    /// <summary>粘贴 PAT 入口：弹窗输入 → 校验 → 保存并触发部署检测。</summary>
    private async Task UsePatAsync()
    {
        if (_cloudBusy) return;
        var dlg = new TextInputDialog("粘贴 GitHub Token（PAT）",
            "1) 打开 https://github.com/settings/personal-access-tokens/new 创建 Fine-grained Token\n" +
            "2) Resource owner 选你的账号；Repository access 勾选 TRAE-Automatic-sign-in\n" +
            "3) Permissions：Actions / Contents / Pull requests / Secrets / Workflows 均选 Read and write\n" +
            "4) 生成后把 github_pat_ 开头的 Token 粘贴到下方");
        if (dlg.ShowDialog(this) != DialogResult.OK) return;
        string pat = dlg.Value ?? "";
        if (string.IsNullOrWhiteSpace(pat)) return;

        _cloudBusy = true;
        _btnUsePat.Enabled = false;
        try
        {
            var v = await _ghApi.ValidatePatAsync(pat);
            if (!v.IsValid)
            {
                SetCloudLog("PAT 校验失败：" + (v.Error ?? "未知错误"));
                return;
            }
            _config.GitHubToken = pat;
            _config.GitHubLogin = v.Login;
            _config.Save();
            SetCloudLog("已使用 PAT 授权：" + v.Login + (v.CanWrite ? "" : "（尚未检测到 fork 仓库，部署时会自动创建）"));
            RefreshCloudState();
        }
        finally
        {
            _cloudBusy = false;
            _btnUsePat.Enabled = true;
        }
    }
```

- [ ] **Step 3: 在 `RefreshCloudState()` 两分支接入显隐控制**

原方法（L87-104）替换为：

```csharp
    private void RefreshCloudState()
    {
        if (!string.IsNullOrEmpty(_config.GitHubToken))
        {
            UpdateCloudActionUi(false);
            _lblCloudState.Text = "已授权" + (string.IsNullOrEmpty(_config.GitHubLogin) ? "" : "：" + _config.GitHubLogin);
            _btnCloudAction.Text = "检测部署状态…";
            _btnCloudAction.Enabled = false;
            _ = RefreshDeploymentStateAsync();
        }
        else
        {
            UpdateCloudActionUi(true);
            _lblCloudState.Text = "尚未授权 GitHub";
            _btnCloudAction.Text = "授权 GitHub";
            _btnCloudAction.Enabled = true;
        }
    }
```

- [ ] **Step 4: 编译确认 0 错误**

Run: `dotnet build TraeCheckin.csproj -c Release`
Expected: `0 个错误`。

- [ ] **Step 5: 全量测试确认仍绿**

Run: `dotnet test`（测试命令见计划头）
Expected: 全部通过（36+）。

- [ ] **Step 6: 提交**

```bash
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" add Forms/MainForm.Cloud.cs
git -C "c:\Users\星梦\Desktop\插件开发\TraeCheckin" commit -m "feat: 云端页增加粘贴 PAT 入口，双按钮并列（网页授权/PAT）"
```

---

## Task 5: 可行度验证与发布准备

**Files:** 无代码改动

- [ ] **Step 1: 端到端手测（真实 PAT）**

1. 启动 `bin\TraeCheckin\TraeCheckin.exe`（先 `Stop-Process -Name TraeCheckin` 防锁，重新 build 后启动）。
2. 进「云端签到」页 → 应看到「尚未授权 GitHub」+ 双按钮。
3. 点「粘贴 Token（PAT）」→ 弹窗粘贴真实 PAT（此处不记录明文，验证时从配置读取）。
4. Expected：日志「已使用 PAT 授权：star620」，主按钮转「检测部署状态…」→ 完成后显示「已部署完成，云端每天北京时间 8:00 自动签到」。
5. 再点「重新部署」验证部署链路可用（fork 跳过 + secret 已存在 + workflow 已启用）。

- [ ] **Step 2: 无效串拒绝路径**

重启应用 → 粘贴 `github_pat_` + 80 个 `x`（无效）。
Expected：日志「PAT 校验失败：Token 无效或已被撤销」，不保存、不切状态。

- [ ] **Step 3: 结果汇报**

向用户汇报：测试通过数、编译结果、端到端验证结果截图级描述、是否可发版 v1.4.5（版本号提升 + Release 另起任务）。

---

## 自审记录（写完后运行）

**Spec 覆盖：**
- TextInputDialog → Task 3 ✅
- ValidatePatAsync + PatValidation → Task 1(类型)/Task 2 ✅
- IsPlausibleToken / ParsePermissionsPush / BuildPatScopeHint → Task 1 ✅
- UI 双按钮 + UpdateCloudActionUi + UsePatAsync → Task 4 ✅
- 存储复用（不改 AppConfig）→ Task 4 Step 2 直接写 `_config.GitHubToken/GitHubLogin` ✅
- 错误处理表 → Task 2 catch + 401/404 分支 + UI 日志 ✅
- 可行度验证（test 绿 / 编译 0 错 / 真实 PAT 端到端）→ Task 5 ✅

**类型一致性：** `PatValidation{IsValid,Login,CanWrite,Error}` 在 Task 1 定义、Task 2 使用、Task 4 读取 `v.IsValid/v.Login/v.CanWrite/v.Error` 一致。`_btnUsePat`/`_cloudActionRow` 字段在 Task 4 Step1 声明、Step2 使用一致。

**已知风险：** `TextInputDialog` 若遇 WinForms 隐式 using 缺失，按编译器提示补 `using System.Drawing; using System.Windows.Forms;`（文件已含）。`UpdateCloudActionUi` 仅控制显隐/跨列，不覆盖各状态已有的文本/Enabled 赋值，回归面最小。
